
		
	// Максимальное нечетное число из массива
	// Arrow-function

	const array = ['4', '3', '7', '10', '1'];

	const sum = array
		.map((el) => parseInt(el))
		.reduce((total, value) => total + value)
	
		console.log(sum);



	
	